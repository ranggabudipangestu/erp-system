## FastAPI backend development environment

# Database connection (local dev)
DATABASE_URL=postgresql+psycopg2://erp_user:erp_password@localhost:5432/erp_system

# CORS
CORS_ORIGINS=http://localhost:3000

# Optional port variable (uvicorn command still sets 5000 by default)
PORT=5000

